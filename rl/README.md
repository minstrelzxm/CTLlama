setup.py – everything around model/tokenizer loading and data prep

train.py – your RL training loop wrapped in a RLTrainer class

utils.py – all standalone helpers and reward functions

run.py – the single‐point script tying them together